[install]
# Only install package versions published at least 7 days ago
minimumReleaseAge = 604800
# Temporary: unblock patched releases for the recent Next.js vulnerability.
minimumReleaseAgeExcludes = ["knip", "next", "react", "react-dom"]
