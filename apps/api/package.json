{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@notra/db": "workspace:*",
    "@unkey/hono": "^2.0.0",
    "hono": "^4.12.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
